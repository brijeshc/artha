{
  "name": "artha",
  "version": "0.1.0",
  "description": "Certified product-meaning for AI coding agents — mine, certify, and serve a team's decisions/conventions/invariants over MCP.",
  "type": "module",
  "license": "MIT",
  "author": "Brijesh Chandrakar",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "artha": "dist/cli.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.0",
    "tsup": "^8.3.5",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
