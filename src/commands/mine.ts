import { logger } from '../util/logger';

/**
 * `artha mine` — draft `decision` entries from git history via the Anthropic
 * API. Implemented in T06.
 */
export async function mineCommand(): Promise<void> {
  logger.warn('`artha mine` is not implemented yet (see task 06).');
}
